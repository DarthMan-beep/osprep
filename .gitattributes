# Normalize everything to LF in the repo and on checkout. Critical: the shell
# scripts and .bats tests run inside Linux containers, where CRLF would break
# the shebang and the grader. This keeps it working on Windows, macOS and Linux.
* text=auto eol=lf

# Binary types that must never be touched.
*.png binary
*.jpg binary
*.ico binary
*.pdf binary
