# Lab 1 — Display two files together

Write the command that displays the contents of **`OS1.txt`** and **`OS2.txt`**
together on the screen — first all of `OS1.txt`, then all of `OS2.txt`.

Both files exist in the current directory.

> **Tip:** `cat` can take more than one file; it prints them in order.
